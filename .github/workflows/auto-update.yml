name: debug
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 查看当前目录所有文件
        run: ls -la

      - name: 查看 Node 版本
        run: node -v

      - name: 查看 ip_all.json 是否存在
        run: cat ip_all.json
